import React from 'react';
import Stub from '../_Stub.jsx';
export default function Calendar() {
  return <Stub icon="Calendar" title="Calendar — port in progress"
    hint="Weekly view, availability windows, blocks, and a public booking link. Coming from the prototype." />;
}
