import React from 'react';
import Stub from '../_Stub.jsx';
export default function Clients() {
  return <Stub icon="Users" title="Clients — port in progress"
    hint="Active / Leads / Paused stages, lifetime value, tags, and notes. Coming from the prototype." />;
}
