import React from 'react';
import Stub from '../_Stub.jsx';
export default function Documents() {
  return <Stub icon="Doc" title="Documents — port in progress"
    hint="DocuSign-style e-signature: upload PDFs or write in-app, drag fields, send to clients, vault + templates." />;
}
