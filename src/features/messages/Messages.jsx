import React from 'react';
import Stub from '../_Stub.jsx';
export default function Messages() {
  return <Stub icon="Chat" title="Messages — port in progress"
    hint="Two-way / broadcast modes, attachments, iMessage-style timestamps, doc and invoice cards." />;
}
